#!/usr/bin/env node
                          

var args = process.argv.concat().splice(2),
ops = {};

ops.command = args.shift();
ops.args = args;

require('../lib-new').execute(ops);   
                    
